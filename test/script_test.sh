#!/bin/bash

#SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
## shellcheck disable=SC2164
#cd "$SCRIPT_DIR"

chmod +x assets/scripts/flutter_code_check_init.sh
sh assets/scripts/flutter_code_check_init.sh
